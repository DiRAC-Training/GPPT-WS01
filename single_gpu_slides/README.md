# Profiling talk for GPPT workshop

Compile talk using quarto command:

```sh
quarto render presentation_v0.qmd --to revealjs
```

The resulting html object must be opened in a browser to read properly (i.e. it doesn't render in github).

I'll convert to pdf when it's a more final version.
